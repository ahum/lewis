(ns lewis-core.core-test
  (:require [clojure.test :refer :all]
            [lewis-core.core :as lc]))

;(deftest one-test
;  (testing "Runs project one"
;    (let 
;      [result  (lc/run-flow "test/one/project/def.json" "dev")] 
;      (is (= result "blah Hello, World!"))))
;      )
